[package]
name = "checkmate_engine"
version = "1.0.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rayon = "1.7"        # Parallel processing

[lib]
crate-type = ["cdylib"]  # For C# FFI binding