# Ruff configuration for paddock-parser-ng
target-version = "py310"
line-length = 88

src = ["src"]

[lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
]
