<!-- desktop_app/MainWindow.xaml -->
<Window x:Class="desktop_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:desktop_app.ViewModels"
        Title="Checkmate V8 - Command Deck" Height="720" Width="1280">
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <ListView ItemsSource="{Binding QualifiedRaces}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="5" Padding="10" CornerRadius="5" BorderThickness="1" BorderBrush="{Binding ScoreColor}">
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayTitle}" FontSize="16" FontWeight="Bold"/>
                            <TextBlock Text="{Binding PostTime, StringFormat='Post Time: HH:mm'}"/>
                            <TextBlock Text="{Binding CheckmateScore, StringFormat='Score: {0:F2}'}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>